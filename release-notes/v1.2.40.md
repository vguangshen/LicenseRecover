# LicenseRecover v1.2.40

修复 .NET 本地注册有效、连接限制可读取，但业务层仍显示“系统不支持任何模式”的注册链一致性问题。

- 大小写两个注册组件同时存在时优先 `ITMC.Regedit.dll` 现代链，小写链仅作为兼容回退。
- Java 协调器、ASP.NET Host 与 Bridge 使用完全相同的选择顺序。
- 保留 v1.2.39 的 `SoftVersionID -> RegStr` 合并逻辑。
- 大写链原生验证成功后，再读取站点根 `config.xml`；真实持久化 `RegStr` 必须能够按当前 `ProName` 解出并包含当前 `SoftVersionID`，否则返回 `[MODE_VERIFY]`。
