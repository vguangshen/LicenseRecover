# v1.2.41 validation policy

The Java one-click path is fail-closed: final OK is emitted only after target-native write and fresh-JVM verification of the startup-derived profile, persisted RegStr tokens, and runtime ProductID/ProName consistency. Failed candidates are rolled back.
