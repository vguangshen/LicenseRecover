name: Apply DS24 data-config primary RegStr 1.2.23
on:
  push:
    branches: [ main ]
    paths:
      - 'scripts/patch-ds24-data-primary-regstr-1.2.23.py'
      - '.github/workflows/apply-ds24-data-primary-regstr-1.2.23.yml'
  workflow_dispatch:
permissions:
  contents: write
jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main
          fetch-depth: 0
      - name: Apply source/test/release patch
        run: python scripts/patch-ds24-data-primary-regstr-1.2.23.py
      - name: Check diff
        run: git diff --check
      - name: Commit patch
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add src/main/java/LicenseRecoverModernGUIJavaPlan.java src/test/java/RefactorSmokeTest.java VERSION.txt README.md release-notes/v1.2.23.md
          git commit -m 'fix: 静态确认DS24 data-config主RegStr并发布v1.2.23'
          git push origin HEAD:main
