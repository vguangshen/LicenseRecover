name: Fix pre-block smoke 1.2.21
on:
  push:
    branches: [ main ]
    paths:
      - 'scripts/fix-preblock-smoke-1.2.21.py'
      - '.github/workflows/fix-preblock-smoke-1.2.21.yml'
  workflow_dispatch:
permissions:
  contents: write
jobs:
  fix:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main
          fetch-depth: 0
      - run: python scripts/fix-preblock-smoke-1.2.21.py
      - run: git diff --check
      - run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add src/test/java/RefactorSmokeTest.java
          git commit -m 'test: 修正pre-block顺序断言'
          git push origin HEAD:main
